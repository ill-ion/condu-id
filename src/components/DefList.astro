---
/**
 * Vertical key/value pairs styled as a journal-style definition list.
 * Pass items as children: each <DefRow> child is one pair.
 */
---

<dl class="grid grid-cols-1 md:grid-cols-[max-content_1fr] gap-x-8 gap-y-3 text-sm">
  <slot />
</dl>
